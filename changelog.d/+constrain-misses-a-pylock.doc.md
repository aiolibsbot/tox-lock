`constrain_package_deps = true` -- the guard the README tells every project installing from a lock to write -- does nothing in an env installing from a `pylock.toml`: `tox` writes the constraints file it reads while installing `deps`, and a `pylock` env installs none. The README now says so, and gives the way in: a requirements-format lock compiled beside the `pylock.toml` out of the same sources, named in `constraints`.
